global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "backend"
    static_configs:
      - targets: ["backend:3000"] # Backend chạy trong Docker
      # - targets: ["host.docker.internal:3000"] # Backend chạy local 